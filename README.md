# Ring Run

婚礼马上开始，婚戒还在路上。用一条连续路线把带着自己头像的戒指盒绕过猫爪、扫地机器人和滚动香槟瓶。

这是空间手势的第二差异样本：路线不是 Guard My Face 的防御墙，而是移动主体真正行走的道路。

```bash
npm install
npm run dev
npm run build
```

QA 可使用 `?scene=1|2|3` 直接进入指定幕；`?user_name=` 与 `?avatar_url=` 覆盖玩家身份。
